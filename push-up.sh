# This file contains Git commands that stage, commit, and push changes to the 'main' branch of the remote repository.

# Stage all changes
git add .

# Commit changes
git commit -m "Update the bash script"

# Push changes to the 'main' branch of the remote repository
git push origin main
